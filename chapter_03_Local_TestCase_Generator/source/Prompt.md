Our main objective is that I will be sharing with you the Jira URL, Jira token, and Jira email ID for you.

What you need to do is create a simple application in Python that has a frontend also. You can use a simple frontend application. In Python, you can use Stimlet or whatever you want to use.

The options will be on the front page, and there will be an option very similar to ChatGPT where I can interact and enter the details. For example, I will ask you to create test cases for this Jira ID, and I will click on the Send button in this case.

After that, you will be able to fetch the details. There will be a second screen in this case where you will have the Jira email ID. You can take the configuration of the Jira email ID, your email ID URL, and all this, and save it also in this case.

Now what you need to do is this: it is a very simple application. By using the Ollama local model, which we are using, which is Gemma 3 1 billion parameter 1, which I already have stored locally, I want you to do one thing. You need to use the Ollama connection, which is already running right now. Create this as it is. If the user does not want to use Ollama, as a fallback, you can use the GROQ also on the cyber side in this case. GROQ is groq.com. I will share the token also, and the token will be available in the setting format in this case.

It is a simple two-page application where the user will ask you to create a test plan based on a Jira ticket. You will fetch the Jira ticket automatically from Jira and create a test case based on the template that is present in your template folder in this case.